# データベース・サマリー作成マニュアル

## ファイル構成

| ファイル | 説明 |
|---|---|
| `fee_data_complete_base_YYYYMMDD.xlsx` | baseファイル（全取引データ） |
| `fee_data_complete_base_YYYYMMDD_summary.xlsx` | 3期サマリー（イントロデューサー別集計） |

---

## baseファイルの列構成（【保管】完璧なデータ シート）

- データは **5行目から** 開始（1〜4行目はヘッダー等）
- 主要列：

| 列 | 内容 |
|---|---|
| A (col1) | アクティビティ日 |
| D (col4) | プラン番号 |
| F (col6) | イントロデューサーコード（Fコード） |
| G (col7) | イントロデューサー名 |
| P (col16) | コミッション額（全額） |
| R (col18) | スプリット②（割合）← イントロ分の比率、または '酒井G' |
| S (col19) | スプリット③（共同募集）← 1.0 または 0.5（共同募集時） |
| U (col21) | スプリット④（Goo分）← Goo分の比率 |
| W (col23) | スプリット⑤（マネージャー分）← マネ分の比率 |
| Y (col25) | グループ |
| Z (col26) | 小グループ |
| AA (col27) | 状態 |
| AB (col28) | 備考 |
| AC (col29) | 備考（特別対応） |

---

## STEP 1：共同募集行の重複スプリット処理

### 目的
共同募集（S列=0.5）のプランは同一プランが2行存在する。Goo分・マネ分が重複してカウントされないよう、偶数行目のU列とW列を空欄にする。

### 処理ルール
1. **S列(col19)=0.5** の行を対象とする
2. D列（プラン番号）でグループ化する
3. 各グループ内で **偶数番目の行**（2行目・4行目・…）の **U列を空欄（None）** にする
4. **U列が空欄の行のW列も空欄（None）** にする

### 注意
- R列（イントロ分比率）は変更しない
- S列は変更しない
- 奇数行目（1行目・3行目・…）はそのまま残す

---

## STEP 2：スプリット未設定行の特定と設定

### 未設定行の定義
以下の条件をすべて満たす行が「未設定」：
- R列（col18）が **数値でない**（None または文字列）
- かつ **R列 ≠ '酒井G'**（酒井Gは別扱い）
- U列=0、W列=0

### 設定値の判断基準
サマリーファイルの **備考（特別対応）列（col23）** を参照して設定する。

| 備考（特別対応）の内容 | R | U | W |
|---|---|---|---|
| 「イントロデューサー分0.75%、Goo分は0.25%」 | 0.75 | 0.25 | 0 |
| 道下Gと同様（標準） | 0.5 | 0.25 | 0.25 |
| イントロデューサーとGooで折半 | 0.5 | 0.5 | 0 |
| 酒井G傘下・区別不可 | '酒井G' | 0 | 0 |

### グループ別スプリット標準値（参考）

| グループ | R | U | W |
|---|---|---|---|
| 10.竹田G（既得確保） | 0.75 | 0.25 | 0 |
| 1.道下G（標準） | 0.5 | 0.25 | 0.25 |
| GA / 個別（R=U=50%） | 0.5 | 0.5 | 0 |
| 酒井G（未計算） | '酒井G' | 0 | 0 |

---

## STEP 3：サマリー生成

### 期間定義

| 期 | アクティビティ日 |
|---|---|
| 1期 | 2025-10-06 |
| 2期 | 2026-01-07 |
| 3期 | 2026-04-05 |

### 計算式

```
T（イントロ分） = P × R × S    ※ R・Sが数値の場合のみ。非数値（酒井G等）は0
V（Goo分）     = P × U        ※ U が空欄（None）の行は 0
X（マネ分）    = P × W        ※ W が空欄（None）の行は 0
```

### サマリー列構成（3期サマリー シート）

| 列 | 内容 |
|---|---|
| A (col1) | Fコード |
| B (col2) | イントロデューサー名 |
| C-F (col3-6) | 1期：件数・T合計・V合計・X合計 |
| G-J (col7-10) | 2期：件数・T合計・V合計・X合計 |
| K-N (col11-14) | 3期：件数・T合計・V合計・X合計 |
| O-R (col15-18) | 3期合計：合計件数・T・V・X |
| S-W (col19-23) | イントロデューサー情報（グループ・小グループ・状態・備考・備考特別） |

### 集計方法
- Fコード別・期別に **件数・T合計・V合計・X合計** を計算
- グループ等の情報はbaseのY〜AC列から取得
- 最終行（合計行）も再計算する

---

## STEP 4：損益確認

### 計算式
```
損益 = ITA→Goo振込分 − Excel計算した分 − 酒井G（未計算） − 不完全データ
```

### 各値の出し方

| 項目 | 算出方法 |
|---|---|
| ITA→Goo振込分 | ITAシステムから取得した実際の振込金額 |
| Excel計算した分 | baseの全行で `T + V + X` を期別に合計（R='酒井G'行は除く） |
| 酒井G（未計算） | baseの R='酒井G' 行の P列を期別に合計 |
| 不完全データ | 不完全データシートのP列を期別に合計 |

### 正常範囲
- 損益が **±100ドル以内** であれば正常
- 損益が大きい場合はスプリット未設定行が残っている可能性がある

---

## チェックリスト

- [ ] S=0.5行の偶数行目のU・W列が空欄になっているか
- [ ] R列が非数値（Noneまたは文字列）の行にスプリットを設定したか
- [ ] 酒井G行（R='酒井G'）はU=W=0になっているか
- [ ] サマリーのT・V・X合計がbaseの集計と一致しているか
- [ ] Sheet1の損益が±100ドル以内か

---

## AIへの指示テンプレート

### baseファイルの前処理を依頼する場合
```
fee_data_complete_base_YYYYMMDD.xlsx の【保管】完璧なデータシートに対して以下を実施してください。
1. S列(col19)=0.5の行をD列(プラン番号)でグループ化し、各グループの偶数行目のU列(col21)を空欄にする
2. U列が空欄の行のW列(col23)も空欄にする
```

### スプリット未設定行の設定を依頼する場合
```
fee_data_complete_base_YYYYMMDD.xlsx で R列(col18)が数値でなく、かつ '酒井G' でもない行を探し、
サマリーの備考（特別対応）列を参照して適切なR・U・W値を設定してください。
```

### サマリー生成を依頼する場合
```
fee_data_complete_base_YYYYMMDD.xlsx（【保管】完璧なデータシート）を元に、
3期サマリーを以下の計算式で再生成してください。
- T（イントロ分）= P(col16) × R(col18) × S(col19)　※R・Sが数値の場合のみ
- V（Goo分）    = P(col16) × U(col21)              ※Uが空欄の行は0
- X（マネ分）   = P(col16) × W(col23)              ※Wが空欄の行は0
Fコード別・アクティビティ日別（1期:2025-10-06、2期:2026-01-07、3期:2026-04-05）に集計し、
合計行も更新してください。
```

### 損益確認を依頼する場合
```
以下の値でアクティビティ日別に損益を計算してください。
損益 = ITA→Goo振込分 − Excel計算した分（T+V+Xの合計） − 酒井G（R='酒井G'行のP合計） − 不完全データのP合計
```
